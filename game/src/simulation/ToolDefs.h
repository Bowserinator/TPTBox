#ifndef TOOL_DEFS_H
#define TOOL_DEFS_H

#include "Simulation.h"

#define PERFORM_FUNC_ARGS Simulation &sim, int i, coord_t x, coord_t y, coord_t z, Particle *parts, pmap_id pmap[ZRES][YRES][XRES], coord_t brushX, coord_t brushY, coord_t brushZ, float brushStrength

#endif